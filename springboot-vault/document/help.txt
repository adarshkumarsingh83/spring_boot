$ docker run -d -p 8200:8200 --name vault -e 'VAULT_DEV_ROOT_TOKEN_ID=myroot' -e 'VAULT_DEV_LISTEN_ADDRESS=0.0.0.0:8200' vault

# Get inside the vault docker container
$ docker exec -i -t vault sh
$ export VAULT_ADDR='http://localhost:8200'
vault auth myroot

http://localhost:8200
select token and value is myroot

click into the secret then -> create secret


Path for this secret=> springboot-vault-app
{
  "espark.key": "defualtprofile",
  "espark.message": "welcome to the espark",
  "espark.value": "espark@defulat"
}

Path for this secret=> springboot-vault-app/localprofile
{
  "espark.key": "localprofile",
  "espark.message": "welcome to the espark",
  "espark.value": "espark@local"
}

Path for this secret=>  springboot-vault-app/prodprofile
{
  "espark.key": "prodprofile",
  "espark.message": "welcome to the espark",
  "espark.value": "espark@prod"
}

## TEst the application by using
$ curl localhost:8080/api/configuration


# to know all running process of docker
$ docker ps

# to kill running process 
$ docker kill <process id>